app:
  name: "dove"
  version: "1.0.0"
  port: 8080
  mode: "debug"

database:
  driver: "mysql"
  host: "117.50.220.90"
  port: 3306
  username: "root"
  password: "dean123"
  database: "dove"
  charset: "utf8mb4"
  parse_time: true
  loc: "Local"
  max_idle_conns: 10
  max_open_conns: 100
  conn_max_lifetime: 3600

redis:
  host: "117.50.220.90"
  port: 6379
  password: "dean123"
  database: 0
  pool_size: 10

log:
  level: "debug"
  format: "json"
  output: "both"   # 输出到所有目标：控制台、错误输出和文件
  file:
    path: "logs/app.jsonl"
    max_size: 1
    max_age: 30
    max_backups: 10
  sql:
    enabled: true
    slow_threshold: 200
    log_level: "info"

jwt:
  secret: "your-secret-key"
  expire_hours: 24

cors:
  allowed_origins:
    - "http://localhost:3000"
    - "http://localhost:8080"
    - "http://localhost:5173"
  allowed_methods:
    - "GET"
    - "POST"
    - "PUT"
    - "DELETE"
    - "OPTIONS"
  allowed_headers:
    - "Content-Type"
    - "Authorization"
  allow_credentials: true

timezone:
  timezone: "Asia/Shanghai" 